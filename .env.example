# Exchanges (public OHLCV works without keys; add for higher limits if available)
BINANCE_API_KEY=
BINANCE_API_SECRET=
HL_API_KEY=
HL_API_SECRET=

# Alerts (Telegram only per preference)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Options
CONFIG_PATH=config/config.yaml
CACHE_PATH=.cache
QUOTE_ASSET=USDT
BACKFILL_LIMIT=1000
BACKFILL_LOOKBACK_BARS=1000
DUCKDB_MIRROR=1

# Rotation scan overrides (optional; leave empty to use config.yaml)
# APEX_MOVE_THRESHOLD_PCT=2.5
# APEX_ALT_LAG_THRESHOLD_PCT=2.0
# APEX_CORR_MIN=0.6
# APEX_BETA_MIN=1.2
# APEX_MOVE_LOOKBACK_BARS=3

# Alerts testing: set to 1 to disable quiet hours suppression (00-06 UTC)
# QUIET_HOURS_OFF=1
